package io.boomerang.model.enums;

public enum TaskType {
  start, end, template, custom, generic, decision, approval, setwfproperty, manual, eventwait, acquirelock, releaselock, runworkflow, runscheduledworkflow, script, setwfstatus
}
